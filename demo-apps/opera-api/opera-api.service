[Unit]
Description=Opera PMS API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/opera-api
ExecStart=/opt/opera-api/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8080
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
